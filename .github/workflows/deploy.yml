name: Deploy Astro to REG.RU

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Install and build
        run: |
          npm ci
          npm run build
          # Копируем .htaccess в client
          cp .htaccess dist/client/

      - name: 🚀 Deploy via Rsync
        run: |
          # Загружаем ТОЛЬКО содержимое dist/client/ в корень сайта
          rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no" \
            ./dist/client/ \
            u3339934@31.31.197.15:/var/www/u3339934/data/www/rus-news.online/
          echo "✅ Сайт успешно задеплоен!"