name: Deploy Astro to REG.RU

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Install and build
        run: |
          npm ci
          npm run build
          # Копируем .htaccess в dist/ (опционально)
          cp .htaccess dist/

      - name: 🔐 Setup SSH Key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🚀 Deploy via Rsync
        run: |
          # Загружаем ВСЮ папку dist/
          rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no" \
            ./dist/ \
            u3339934@31.31.197.15:/var/www/u3339934/data/www/rus-news.online/
          echo "✅ Сайт успешно задеплоен!"