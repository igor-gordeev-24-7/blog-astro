name: Deploy Astro to REG.RU

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Install and build
        run: |
          npm ci
          npm run build

      - name: 🔐 Setup SSH Key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 📁 Create website directory on server
        run: |
          ssh -o StrictHostKeyChecking=no u3339934@31.31.197.15 \
            "mkdir -p /www/rus-news.online && echo '✅ Папка создана'"

      - name: 🚀 Deploy via Rsync
        run: |
          rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no" \
            ./dist/ \
            u3339934@31.31.197.15:/www/rus-news.online/
          echo "✅ Сайт успешно задеплоен!"