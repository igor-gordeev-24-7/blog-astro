RewriteEngine On
RewriteBase /

# 1. Если файл существует - отдаем его
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 2. Все остальное - ищем в папке client/
RewriteRule ^(.*)$ /client/$1 [L]