---
interface Props {
  src: string;
  alt?: string;
  width?: string;
}

const { src, alt = '', width = '100%' } = Astro.props;
---

<img 
  src={src} 
  alt={alt} 
  style={`width: ${width}; max-width: 100%; height: auto; border-radius: 8px;`}
  loading="lazy"
/>